# Implementation Plans

Generated by the improve skill on 2026-07-05, at commit `0a61e06` (branch
`codex/find-broken-app-issues`). Execute in the order below unless
dependencies say otherwise. Each executor: read the plan fully before
starting, honor its STOP conditions, and update your row when done.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001  | Verification baseline (typecheck, lint, tests) | P1 | M | — | DONE |
| 002  | Ranged gangster slides while recharging | P1 | S | 001 | DONE |
| 003  | Enemy subclass initialization order | P1 | M | 002 | DONE |
| 004  | Death takes precedence over victory | P2 | S | 001 | DONE |
| 005  | Floor spans full level width | P2 | M | 001 | DONE |
| 006  | Remove dead code, consolidate constants | P2 | M | 002, 003, 004 | DONE |
| 007  | Fix stale CLAUDE.md / README | P2 | S | 001, 006 | DONE |
| 008  | Music instance leak on scene restarts | P3 | S | 001 | DONE |
| 009  | Progression & persistence design spike | P3 | M | 001 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale)

## Dependency notes

- **001 first, always.** It creates the `typecheck`/`lint`/`test` scripts every other plan uses as verification gates, and fixes the 2 pre-existing type errors that would otherwise mask new ones.
- **002 before 003**: both restructure `Enemy.ts`'s animation-complete handling; 003 is written assuming 002's `onAnimationComplete`/`resumePatrol` methods exist.
- **006 after 002/003/004**: 006 deletes `src/scenes/1-LevelOne/` and consolidates `Batman.ts` constants; landing the bug fixes first avoids merge conflicts in the entity files.
- **007 last of the code-adjacent plans**: docs must describe the tree as it exists after 006's deletions and 001's scripts.
- **005, 008, 009** are independent of each other and of 002/003 (005 touches `LevelLoader`/level JSONs; 008 touches scene music lines; 009 is additive).

## Findings considered and rejected

- **`worldHeight: 384–500` in level JSONs looks like a camera-breaking bug** — verified against Phaser's `BaseCamera.clampY` (`Math.max` of the limits pins scrollY to 0); it's dead-but-harmless data. Noted inside plan 005; do not "fix" it separately.
- **Phaser 3.60 → 3.9x upgrade** — real but not urgent; no APIs in use are deprecated, and the regression-test surface (none) makes it poor value right now. Revisit after a verification baseline and some entity tests exist.
- **`npm audit` high-severity advisories (11)** — all in the dev-only ESLint 8 chain; resolved as a side effect of plan 001's ESLint 9 migration, not a standalone security finding. Production dependency audit is clean.
- **Punch can hit the same enemy twice per swing** (733ms punch animation vs 400ms hit-stun window; a 3-HP enemy dies in 2 punches) — plausibly intended game feel; flagged for the maintainer, not planned. Tune `ENEMY.HIT_STUN_MS` in `src/constants/physics.ts` if unwanted.
- **`DialogueBox`, `UIAssets`, `BATARANG` asset are unused** — kept deliberately (staged future work: NPC dialogue, UI-kit integration, secondary attacks). Plan 006 documents them as intentional; direction candidates below.

## Direction candidates (not planned; grounded options for the maintainer)

- **Finish the UI-kit / NPC dialogue integration** — `DialogueBox` and `UIAssets` are fully built and unused; HUD had an image-bar branch waiting for them; level JSONs carry narration while NPCs are silent.
- **Secondary attacks / batarang** — keys 2–4 already bound in `controls.utils.ts:247-250`; batarang asset registered in `assets.ts`.
- **Day/Night level variety** — `ParallaxBackground` fully supports `'Day' | 'Night'`; `GameLevel.ts` hardcodes `'Night'`; one level-JSON field away.
- (Progression/persistence was selected and is plan 009.)
